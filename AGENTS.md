このファイルは、このプロジェクトに関わるエージェント（AI）向けの指示書です。

## コミュニケーション

* ユーザーとのすべてのコミュニケーション、プルリクエスト（PR）のコメントなどは、すべて日本語で行ってください。

## プロジェクト概要

* プロジェクト名: G2M-Export
* 目的: ローカルの Git リポジトリをスキャンし、AIが理解しやすい「1ファイル統合 Markdown」へエクスポートする。
* 参照プロジェクト: C2M-Export および J2M-Export のアーキテクチャパターンとファイル命名規則に従う。

## 開発ルールと技術仕様

### 1. 依存関係と環境構築

* ランタイム依存関係は `requirements.txt` に記載されています。
* 開発専用の依存関係（pytest など）は `requirements-dev.txt` に記載されています。
* 開発環境のセットアップは `pip install -r requirements.txt -r requirements-dev.txt` を実行してください。
* テストの実行は `PYTHONPATH=. python3 -m pytest tests/` を使用してください。

### 2. 設定管理

* 設定は YAML ファイルで管理します。
* デフォルトの設定ファイル名は `g2m_config.yaml` です。
* 設定ファイルの検索優先順位:
    1. `--config` で明示的に指定されたパス
    2. コマンドライン引数 `src_dir` で指定されたスキャン対象ディレクトリ直下の `g2m_config.yaml`
    3. カレントディレクトリの `g2m_config.yaml`
* `g2m_config_sample.yaml` をテンプレートとして提供し、ユーザーが `g2m_config.yaml` を作成して利用する運用を推奨しています。

### 3. スキャンとバイナリ判定ロジック

* ファイルのスキャンは `Scanner` クラス（`scanner.py`）で行い、再帰的に探索します。
* バイナリ判定は以下の二段階で行ってください：
    1. 拡張子ベース: `g2m_config.yaml` の `binary_extensions` に含まれるか。
    2. 内容ベース: ファイルの先頭部分にヌルバイト（`\x00`）が含まれているか、または UTF-8 としてデコードに失敗するか。
* `.git` ディレクトリ配下は常にスキャンの対象外とします。

### 4. Git メタデータの取得（制約事項）

* **最重要制約**: `git` コマンドラインバイナリに依存せず、`.git` ディレクトリ内のファイルを直接読み取って情報を抽出してください。
* 抽出対象:
    * リモートURL: `.git/config` から `[remote "origin"]` の `url` を取得。
    * カレントブランチ: `.git/HEAD` から取得（`ref: refs/heads/` 以降、またはコミットハッシュ）。
* リモートURLの変換:
    * SSH 形式を HTTPS 形式に変換。
    * プロバイダー（GitHub, Bitbucket, GitLab等）に応じた適切な Web ビュー URL を生成。

### 5. Markdown 生成

* ディレクトリの深さに応じて、見出しレベル（`#`, `##`, ...）を動的に変更し、プロジェクト構造を視覚化してください。
* ファイルパス、リモートURL（存在する場合）、ファイル内容の順で構成します。
* 出力ファイル名:
    * Git リポジトリの場合: `【Repo】 <リポジトリ名>.md`（Bitbucket Server の場合は `【Repo】 <プロジェクトキー>_<リポジトリ名>.md`）。
    * 非 Git ディレクトリの場合: `【Dir】 <ディレクトリ名>.md`。

### 6. コード品質

* `.pre-commit-config.yaml` で設定された pre-commit フック（Ruff, trailing-whitespace 等）を必ず使用してください。
* CI 連携（`.pre-commit-ci.yml`）により自動化された品質管理を維持してください。

### 7. コメントとドキュメント

* コード内のコメントや docstring はすべて日本語で記載してください。
* docstring は PEP 257 に準拠し、クラスや関数の役割を明確に説明してください。
* 実装の意図や背景を重点的に記載し、自明なコードに対する重複したコメントは避けてください。

### 8. ログ出力とエラーメッセージ

* ログ出力（INFO, WARNING, ERROR すべて）は日本語で行ってください。
* 例外処理では `Exception` による広範なキャッチを避け、`OSError` や `yaml.YAMLError` など、発生が想定される具体的な例外型を明記してください。
* エラーメッセージは、以下の構成を維持してください：
    * 現象: 何が起きたか
    * 原因: なぜ起きたか（可能な限り `詳細: {e}` を含める）
    * 対処方法: ユーザーが次に何をすべきか
    * 例: `設定ファイルの読み込みに失敗しました（現象）。g2m_config.yamlの内容を確認してください（対処方法）。詳細: {e}（原因）`

### 9. 今後の拡張性

* 現在、リポジトリ構成の解析（プロジェクトキーの抽出等）は Bitbucket Server (Data Center) 形式を主眼に置いていますが、将来的に Cloud 版への対応も計画されています。
* 新たな機能を設計・実装する際は、特定の環境に依存しすぎず、将来的なサポート対象拡大を見据えた抽象化や設計（インターフェースの分離等）を検討してください。

### 10. README の保守

* 新機能の追加や仕様変更を行った際は、必ず `README.md` および `README_DEV.md` を更新してください。

## 運用指示（ディーププランニングモード）

* 変更を行う前に、必ず「ディーププランニングモード」を開始してください。
* ユーザーと対話し、要件を完全に理解するまで（不確実性がゼロになるまで）質問を繰り返してください。
* 計画の承認を得る前に、前提条件や期待される結果についてユーザーの確認を取ってください。
* 計画が承認された後は、自律的に実行してください。
